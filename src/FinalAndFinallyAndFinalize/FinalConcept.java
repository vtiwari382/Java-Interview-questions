package FinalAndFinallyAndFinalize;

public class FinalConcept {

	public static void main(String[] args) {
		int i = 10;   // final i = 10;
		i = 20;
		i = 30;
		System.out.println(i);

	}

}

/*
//Note: 1. If we declare any variable final then only that value will execute in output, otherwise latest value will execute.  
        2. if we define any variable final then that will become constant value.
        3. In case of In-heritance concept if we declare any parent method final then we can't access that method.
        4. In case of In-heritance concept if we declare any parent class final then we can't extends that in-heritance.\
        
  
*/  
  